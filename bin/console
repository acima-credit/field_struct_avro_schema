#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'field_struct_avro_schema'

ROOT_PATH = Pathname.new File.expand_path(File.dirname(File.dirname(__FILE__)))

require_relative '../spec/support/models'

require 'pry'
Pry.start
